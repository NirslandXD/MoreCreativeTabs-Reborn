# Forge build for 1.19.3-1.19.4

Forge is built as one combined `1.19.3-1.19.4` target, matching the Fabric build. Run `gradlew.bat build` from the project root. The resulting artifact is placed in `Forge/build/libs` and `build/libs/Forge`.
